{
  "name": "hussein-pos-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon src/index.js",
    "build": "npx prisma generate && cd ../client && npm install && npm run build",
    "postinstall": "npx prisma generate",
    "seed": "node prisma/seed.js",
    "start": "npx prisma db push && node src/index.js"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prisma": "^6.0.0"
  }
}
